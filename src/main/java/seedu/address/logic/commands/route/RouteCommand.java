package seedu.address.logic.commands.route;

import seedu.address.logic.commands.Command;

/**
 * Commands regarding routes, i.e. listing routes, route creation,
 * and adding orders to routes.
 */
public abstract class RouteCommand extends Command {

    public static final String COMMAND_WORD = "/route";
}
